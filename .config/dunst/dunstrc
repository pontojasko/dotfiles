[global]
    # --- Aparência e Geometria ---
    # CORREÇÃO: Sintaxe nova usa vírgula (x, y)
    origin = bottom-right
    offset = (20, 20)

    # Tamanho da notificação
    width = (300, 450)

    # CORREÇÃO: Altura dinâmica (min, max).
    # (0, 300) significa que ela cresce conforme o texto até 300px.
    height = (0, 300)

    # Espaçamento
    gap_size = 8
    frame_width = 1

    # Quadrado (sem arredondamento)
    corner_radius = 0

    # --- Fonte e Ícones --- Caskaydia Cove, etc)
    font = CaskaydiaCove Nerd Font Mono 9

    icon_position = left
    min_icon_size = 48
    max_icon_size = 48
    text_icon_padding = 12

    # --- Comportamento ---
    transparency = 0
    sort = yes
    idle_threshold = 120
    line_height = 0
    markup = full
    padding = 12
    horizontal_padding = 12
    separator_height = 2
    show_age_threshold = 60
    word_wrap = yes
    ignore_newline = no
    stack_duplicates = true
    hide_duplicate_count = false
    show_indicators = yes

    # Barra de progresso
    progress_bar = true
    progress_bar_height = 8
    progress_bar_frame_width = 0
    progress_bar_min_width = 150
    progress_bar_max_width = 300

# --- Cores GRUVBOX ---
[urgency_low]
    background = "#282828"
    foreground = "#ebdbb2"
    frame_color = "#928374"
    timeout = 4

[urgency_normal]
    background = "#282828"
    foreground = "#ebdbb2"
    frame_color = "#d79921"
    timeout = 6

[urgency_critical]
    background = "#cc241d"
    foreground = "#ebdbb2"
    frame_color = "#fb4934"
    timeout = 0
